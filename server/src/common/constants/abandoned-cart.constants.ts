/** Hours without cart activity before treating as abandoned (matches automation scheduler). */
export const ABANDONED_CART_IDLE_HOURS = 2

/** Ignore carts older than this when scanning for abandoned automation. */
export const ABANDONED_CART_MAX_AGE_DAYS = 14
