import POSDashboard from '@/features/admin/pos/components/POSDashboard';

export default function POSPage() {
    return (
        <div className="h-full">
            <POSDashboard />
        </div>
    );
}
