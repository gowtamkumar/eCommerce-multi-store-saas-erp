# Production Environment Variables
NODE_ENV=production

# MongoDB Credentials (CHANGE THESE!)
MONGO_USERNAME=admin_ecommerce_landing
MONGO_PASSWORD=admin_1995

# MongoDB Connection
MONGODB_URI=mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@mongodb:27017/ecommerce?authSource=admin

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3001
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXTAUTH_SECRET=tTeIaOdVHFbCMhKB4HghrCnQth1CCl2nRRyum6N8VzE=

# Mongo Express Admin Interface (Optional - Remove in production for security)
ME_ADMIN_USERNAME=admin
ME_ADMIN_PASSWORD=admin_1995

# Database Seeding (Set to true only for initial setup, then set to false)
SEED_DB=false

# SSLCommerzPayment
STORE_ID=""
STORE_PASSWORD=""
IS_LIVE=false